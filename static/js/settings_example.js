// Settings
omegleSettings = {
    // Default topics
    defaultTopics: [
        'noMultiRP ',   // This topic will cause the bots to disconnect if they connect to each other
        'rp',
        'roleplay'
    ].join(),

    // Extra params to add
    bonusParams: {
    },

    // College stuff
    // college: '',
    // college_auth: '',
}
